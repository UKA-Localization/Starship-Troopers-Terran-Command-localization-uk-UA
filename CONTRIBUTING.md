# Як долучитися

## Модель гілок

```
main  ────●──────●────────●──────   актуальний стан; релізи — теги 6.5.0-1, 6.5.0-2, …
           \    /          /
translate/ui ●──●          /       гілка під задачу → PR у main → гілка видаляється
fix/typos        ●───●───●
```

- **`main`** — єдина постійна гілка. Прямі коміти заборонені, лише через PR.
- **`translate/<що>`**, **`fix/<що>`** — гілки під конкретну задачу; створюються від `main`, після злиття видаляються.

Реліз: тег `<версія-гри>-<номер>` на `main`, запис у [CHANGELOG.md](CHANGELOG.md), файл у Releases.

## Воркфлоу

1. Перегляньте [translation/glossary.tsv](translation/glossary.tsv). Терміни з глосарію перекладаються **тільки** так, як там зазначено. Новий термін або зміна — правка `glossary.tsv` окремим PR.
2. Виберіть файл/місію в issues (або створіть issue, щоб ніхто не робив те саме паралельно).
3. Від `main` створіть гілку `translate/<назва-файлу-або-місії>` або `fix/<коротко-що>`.
4. Перекладайте у [translation/strings.tsv](translation/strings.tsv): заповнюйте колонку `translation`, колонки `file`, `key`, `original` не чіпайте. `source/` не редагується.
5. Перевірте в грі: `python tools/build.py --install` (з `--drafts`, щоб побачити й чернетки), у грі Options → Language → Українська → перезапуск.
6. Відкрийте PR **у `main`**. В описі вкажіть, що перекладено і чи є спірні місця.

## Правила для PR

- Один PR — одна логічна частина (напр. один файл `Language/english_N.csv` або одна місія `Scenarios/<назва>/`). Великі PR важко рецензувати.
- Колонка `status`: порожня — переклад готовий; будь-що інше — чернетка, що в збірку не потрапляє (`fuzzy` — не вичитано; `unresolved` — у рядку є термін без затвердженого перекладу в глосарії). Приймаючи чернетку, очистіть `status`.
- Глосарій: `id` не змінювати (на нього посилається колонка `entities`); `status` порожній — термін затверджено, `fuzzy` — переклад не вичитано, `new` — перекладу ще нема. Затвердження — очистити `status`.
- Перед перекладом прочитайте [translation/STYLE.md](translation/STYLE.md); контекст рядка — у колонці `context`. Якщо контексту бракує або він хибний — правте колонку, це теж внесок.
- Не змінюйте ключі, плейсхолдери (`{0}`, `{1}`), теги форматування (`<color=#FFEBBB>…</color>`, `<b>`, `<i>`), `\n` і підписи клавіш/станів у квадратних дужках (`[Alarm]`, `[ACTIVE]`).
- Стежте за довжиною коротких рядків інтерфейсу (кнопки, назви юнітів, вкладки) — текст не має вилазити за рамку.
- Не перекладайте те, чого немає в оригіналі, і не додавайте відсебеньок.
- Спірні варіанти залишайте коментарем у PR, а не в тексті перекладу.

## Автоматичні перевірки

На кожен PR у `main` GitHub Actions перевіряє, що `translation/strings.tsv` відповідає `source/` (тобто після правок запущено `extract.py`) і що локалізація збирається (`build.py --drafts`). Перевірка плейсхолдерів і тегів у перекладах — на боці конвеєра. Червоний PR не зливається.

## Рецензування

Кожен PR перевіряє щонайменше одна інша людина: стиль, орфографія, збереження технічних елементів, узгодженість термінів з уже перекладеним.
